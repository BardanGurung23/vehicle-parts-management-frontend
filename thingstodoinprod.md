
# to cleanup redis AOF done
# crontab -e
# 0 */2 * * * redis-cli BGREWRITEAOF done 
# auto cleanup image and build cache in cron job done 
# auto backup mysql done
# write shell script to auto fetch , pull, run docker compose up --build -d done
# commit the pnpm lockfile
# checking deploy.sh



# fix healthcheck
# stripe webhook setup for deployment